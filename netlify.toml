[build]
  command = "jekyll build && mv _site/blog/{_redirects,404.html} _site"
  publish = "_site"


# Deploy Preview context: All Deploy Previews
# will inherit these settings.
[context.deploy-preview]
  command = "jekyll build -D && mv _site/blog/{_redirects,404.html} _site"

